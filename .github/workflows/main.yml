name: Daily Commit Job

on:
  schedule:
    - cron: '10 4 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  commit-job:
    runs-on: ubuntu-latest


    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # This step must include your email in the name
      - name: 23f3003281@ds.study.iitm.ac.in
        run: |
          echo "Daily heartbeat commit - $(date)" >> daily-log.txt

      # - name: Commit and push changes
      #   run: |
      #     git config --local user.name "github-actions[bot]"
      #     git config --local user.email "github-actions[bot]@users.noreply.github.com"
      #     git add daily-log.txt
      #     git commit -m "Daily update: $(date -u '+%Y-%m-%d %H:%M:%S') [skip ci]" || echo "No changes to commit"
      #     git push
