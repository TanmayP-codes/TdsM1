name: Test Email Step

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f3003281@ds.study.iitm.ac.in
        run: echo "Hello, world!"
      - name: Checkout code
        uses: actions/checkout@v3

      
